description: Confirm explicit external plan submission reaches the bound coordinator and renders in the review UI.
edges: []
goal: Verify external plan ownership
implementationGroups:
  - id: runtime-proof
    taskIds:
      - verify-ownership
lifecycle: draft
planId: external-ownership-runtime-check
revision: 1
tasks:
  - acceptanceCriteria:
      - The plan is created only with the explicit external-file override.
    description: Check coordinator ownership metadata and browser rendering.
    id: verify-ownership
    title: Verify ownership
    validationIntent: Inspect the CLI response and plan review page.
version: '1'
