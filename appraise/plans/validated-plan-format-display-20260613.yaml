description: Confirm that a plan created through the refreshed Appraise MCP renders its short title, separate description, structured tasks, acceptance criteria, validation intent, dependency, and implementation group correctly.
edges:
  - from: check-header
    to: check-structure
    type: blocks
goal: Validate the refreshed plan display
implementationGroups:
  - id: display-check
    taskIds:
      - check-header
      - check-structure
lifecycle: awaiting_plan_review
planId: validated-plan-format-display-20260613
revision: 1
tasks:
  - acceptanceCriteria:
      - The title is prominent and complete.
      - The description is visible separately and is not rendered as raw data.
    description: Verify the short title and supporting description appear as distinct readable elements.
    id: check-header
    title: Check the plan header
    validationIntent: Compare the rendered plan header with the MCP payload.
  - acceptanceCriteria:
      - Both tasks appear with their descriptions.
      - Acceptance criteria and validation intent appear under the correct task.
      - The blocking edge and implementation group are represented.
    description: Verify task metadata and relationships are presented clearly on the review surface.
    id: check-structure
    title: Check structured task details
    validationIntent: Inspect the task list and graph representations in the browser.
version: "1"
