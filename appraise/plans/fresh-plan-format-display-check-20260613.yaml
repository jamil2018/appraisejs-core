description: Test the refreshed Appraise MCP plan contract and confirm that its review page displays the short title, separate description, structured tasks, acceptance criteria, validation intent, dependencies, and implementation grouping correctly.
edges:
  - from: verify-header
    to: verify-task-details
    type: blocks
goal: Verify the refreshed plan format
implementationGroups:
  - id: display-verification
    taskIds:
      - verify-header
      - verify-task-details
lifecycle: awaiting_plan_review
planId: fresh-plan-format-display-check-20260613
revision: 1
tasks:
  - acceptanceCriteria:
      - The title is displayed prominently.
      - The description appears separately without truncation or raw JSON.
    description: Confirm the concise title and supporting description render as separate, readable content.
    id: verify-header
    title: Verify the plan header
    validationIntent: Compare the rendered header with the MCP payload.
  - acceptanceCriteria:
      - Both tasks are displayed in order.
      - Acceptance criteria and validation intent are associated with the correct task.
      - The blocking relationship and implementation group are understandable.
    description: Confirm task descriptions, acceptance criteria, validation intent, dependencies, and grouping render clearly.
    id: verify-task-details
    title: Verify task structure
    validationIntent: Inspect every structured section on the review page.
version: "1"
