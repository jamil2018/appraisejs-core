# Slice 01: Lifecycle Foundation And Clean Cutover

Add immutable requirement-analysis and validation-design revisions, methodology bindings, execution consent, and
assessment findings. Preserve shared authored tests, Step Definitions, locators, environments, and reports while
resetting incompatible unreleased Quality lifecycle rows in foreign-key-safe order.

Acceptance:

- The schema expresses exact analysis → obligation → design → validation provenance.
- New targets default to `ALWAYS_ASK` execution consent.
- Assessment execution manifests, consents, findings, and decisions are hash-bound.
- Fresh and migrated databases pass Prisma validation and focused migration tests.
