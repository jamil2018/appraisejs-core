# Slice 05: Realization, Preflight, And Execution Preview

Mechanically resolve an approved design into strict Step Definition, operation, locator, environment, data, and matrix
bindings. Compilation cannot change approved semantics. Produce an immutable execution preview and manifest before
any managed run is created.

Acceptance:

- Broad `unknown` realization inputs are replaced with versioned strict contracts.
- Preflight verifies closure, ownership, selected-scenario counts, credentials, side effects, and runtime integrity.
- Semantic drift returns to design review; mechanical repair stays within realization.
