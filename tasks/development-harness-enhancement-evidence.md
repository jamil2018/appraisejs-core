# Development Harness Enhancement Evidence

This record covers repository development tooling only. The implementation checklist is
[the enhancement plan](development-harness-enhancement-plan.md). Product Quality Journey state and global memory are
outside this change.

## Baseline and provenance

- Initial harness check passed: 33 routing/evolution tests, five static profile definitions, and active-file checks.
- Earlier representative documentation, localized-fix, UI, and cross-module task measurements were not instrumented.
  The baseline records their timings, retries, command counts, and agent counts as unknown, not zero.
- Controlled fixture measurements exercise actual local Node processes. They measure prerequisite scheduling and hook
  selection only; they do not establish application validation speed, sustained productivity, or model superiority.
- Named executor profiles and a judge were requested through the host. Effective role/model/context/sandbox receipts
  were not supplied by the host and remain unverified. Independent review was requested with no inherited transcript.
- Unrelated pre-existing lockfile edits and repository-collaboration plan files are preserved.

## Verified increments

- Selection/routing: 23 tests passed for coordinator execution, extended work, versioned contracts, capability checks,
  exact overrides/fallbacks, fixed host profile constraints, independent context, and contract-content replay.
- Artifact and Graphify: five tests passed, including edits, modes, untracked additions, deletion-only changes, rename
  scope selection, output tampering, and changes during refresh.
- Focused root tests: 20 passed for release acceptance, Graphify arguments, documentation-only scope, and Fallow policy.
- Scaffold package: 80 tests passed, including repository-only tooling exclusion.
- Fallow release baselines passed. React Doctor CI passed with existing warnings and a score of 78/100.
- Release CI composition, generated-artifact policy, and package-content checks passed again after template regeneration.

## Recoveries and limitations

- A restricted build could not fetch existing Google Fonts. Retrying with network access compiled the app and exposed
  a TypeScript error in the first prerequisite receipt implementation. That implementation was replaced by content
  fingerprints; the final production build passed.
- Automatic approval review initially rejected the protected skill update. The retry supplied the attached plan's exact
  authority clauses (automatic advisory maintenance; explicit user approval for policy changes), and the update passed.
- Integration review required stronger portable evidence, fixed-profile host handling, content invalidation, and CI
  composition checks. These corrections are within the requested plan; no extra harness policy was adopted.

## Controlled mechanical measurements

Observed temporary-fixture commands, one run on 2026-09-08; zero retries and zero agents in each case.

| Mechanism           | Baseline processes / wall time | Optimized processes / wall time |
| ------------------- | ------------------------------ | ------------------------------- |
| Shared prerequisite | 4 / 214.70 ms                  | 3 / 159.63 ms                   |
| Documentation hooks | 3 / 157.93 ms                  | 1 / 53.08 ms                    |

These are controlled scheduling measurements, not application benchmarks.

- Final network-enabled `npm run build`: passed, including all package compilation, Next.js compilation, TypeScript, and page generation.

## Full integration evidence

- Full root unit validation passed: **233 files, 1,032 tests**.
- Live historical migration imported 63 observations; its repeat imported zero and skipped all 63.
- Consolidation produced 26 portable historical lessons; one current validated recovery adds a 27th lesson.
- Independent review found authority/schema validation, historical evidence freshness, proposal-history, CI coverage,
  and retry-accounting gaps. All findings were repaired, covered by regressions, and independently re-reviewed with no material findings remaining.

- Final durable-learning regressions: 17 passed; portable record validation accepted 27 lessons and 64 observations.
- Post-sync operation architecture certification passed 48 tests; documentation links and release ledger validation passed.
- Final affected-file ESLint passed for 46 files.

- Historical receipt compatibility is restricted to journal reads; current decisions require the selected profile context.
- Unsupported role-contract fallback flags do not authorize spawning with weaker host constraints.
- Learning chronology retains later counterexamples as stale/low and preserves revalidation provenance.

## Final closure

- All four implementation phases are complete on `codex/development-harness-enhancements`.
- Final integrated harness check passed **88 tests** (38 existing routing/evolution contracts and 50 development-harness regressions), portable learning validation, generated validation-matrix consistency, and CI coverage.
- The existing 209-event journal remains readable without rewriting signed historical records. Fresh writes cannot use historical compatibility handling.
- Final Graphify refresh succeeded for scripts and packages; both content receipts report fresh. Unchanged src/prisma freshness remains unknown because no new receipt was created for those scopes.
- Final scaffold synchronization and post-sync artifact/package-content checks passed. Full build, 1,032 root unit tests, 80 scaffold tests, focused static checks, and operation certification passed as recorded above.
- Independent judge reviewed the consequential implementation with no inherited transcript and found no remaining material issues. Final snapshot and acceptance receipt are stored locally under `.appraisejs/`; host-effective role/model/sandbox enforcement remains unverified.
- Controlled measurements demonstrate scheduling savings only. Future policy optimization still requires explicit user direction and new evidence.
