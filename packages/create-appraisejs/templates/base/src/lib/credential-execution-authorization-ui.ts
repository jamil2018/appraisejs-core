/** Explicitly marks the read-only browser request that bootstraps a local
 * credential-authorization session. It is never accepted for a mutation. */
export const CREDENTIAL_AUTHORIZATION_UI_BOOTSTRAP_HEADER = 'x-appraise-ui-bootstrap'
export const CREDENTIAL_AUTHORIZATION_UI_BOOTSTRAP_VALUE = '1'
